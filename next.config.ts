import type { NextConfig } from "next";

/**
 * Konfigurasi Next.js RigelStore.
 * Fase 0: konfigurasi minimal. Modul-modul (payments, storage, bot, AI, social)
 * ditambahkan di fase berikutnya sesuai ARCHITECTURE.md.
 */
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
